<div class="jumbotron">
  <h1>세상에 단 하나뿐인<br>
    나만을 위한</h1>
  <h2>나만의 악세사리</h2>
</div>

<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-md-3">
        <div class="thumbnail">
          <%= link_to image_tag(listing.image.url), listing %>
          <div class="caption">
            <h3><%= listing.name %></h3>
            <p><%= number_to_currency(listing.price) %></p>
            <p><%= "판매자 #{listing.user.name}" %></p>
          </div>
        </div>      
      </div>
    <% end %>
  </div>
</div>

<p id="notice"><%= notice %></p>

<br>

<% if user_signed_in? %>
<%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>








